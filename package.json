{
  "name": "mitrinium-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-editor.mjs",
    "export:mechanics-v18": "node scripts/export-mechanics-catalog-v18.mjs",
    "dev": "npm run build && wrangler dev",
    "test": "node --test",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "jose": "^6.1.0"
  },
  "devDependencies": {
    "pdfmake": "^0.2.20",
    "wrangler": "^4.27.0"
  }
}
